# MNIST CNN 提交说明

## 最终结果

| 项目 | 数值 |
|---|---:|
| 测试集 Macro OvR AUC | 0.9998232110 |
| 可训练参数数量 | 6,330 |
| 测试集准确率 | 98.47% |

## 模型结构

模型名称：`CompactCNN-8-16-32-GAP`

```text
输入：1 × 28 × 28
Conv2d(1, 8, 3×3) + BatchNorm + ReLU + MaxPool
Conv2d(8, 16, 3×3) + BatchNorm + ReLU + MaxPool
Conv2d(16, 32, 3×3) + BatchNorm + ReLU
Adaptive Average Pooling
Linear(32, 10)
```

## 数据与评估流程

- MNIST官方训练部分共60,000张图片。
- 固定划分54,000张训练图片和6,000张验证图片。
- 使用验证集完成模型选择，不使用测试集调参。
- 模型锁定后，对官方10,000张测试图片执行唯一一次评估。
- 多分类AUC采用 Macro One-vs-Rest 方式计算。

## 模型选择理由

该模型只有6,330个可训练参数。验证集AUC为0.999753，在参数量约为最大候选
模型26%的情况下，仅损失约0.000143验证AUC，因此被选为参数效率最好的候选。

## 文件说明

- `02_提交结果.csv`：适合汇总到Excel或绘制散点图。
- `recommended_model.pt`：最终模型权重。
- `train_validate.py`：模型定义及训练验证代码。
- `final_test_result.json`：一次性测试的完整数值及混淆矩阵。
- `training_curves.png`：推荐模型训练曲线。
- `parameter_efficiency.png`：候选模型参数效率比较图。
